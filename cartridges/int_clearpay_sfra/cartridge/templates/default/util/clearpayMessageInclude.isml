<isif condition="${pdict.apApplicable == true}">
    <isinclude template="util/modulesClearpay"/>

    <isclearpaymessage
        classname="${pdict.classname}"
        belowthreshold="${pdict.belowthreshold}"
        brand="${pdict.apBrand}"
        totalprice="${pdict.totalPrice}"
    />
</isif>
